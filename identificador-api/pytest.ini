[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: live frontend/backend/SerpAPI (run with: pytest -m integration)
addopts = -m "not integration"
